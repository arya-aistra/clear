"""Data-free knowledge distillation (DFKD) from Silero v5.

Synthetic audio generator + Silero teacher (soft labels) + boundary-weighted distillation
loss + two-stage trainer. No external labeled dataset is used.
"""
